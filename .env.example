# Database URL (SQLite default for local dev, PostgreSQL for production)
DATABASE_URL="file:./dev.db"

# NextAuth secret & URL
NEXTAUTH_SECRET="your-nextauth-secret-key-at-least-32-chars"
NEXTAUTH_URL="http://localhost:3000"

# Optional Cloud Storage (Supabase / S3 bucket details if not using local upload)
# SUPABASE_URL=""
# SUPABASE_ANON_KEY=""
# SUPABASE_STORAGE_BUCKET=""
